
alt + t ; {h,j,d,p,s,n}
	{alacritty -e btop,brave &,LIBVA_DRIVER_NAME=i915 discord --enable-gpu-rasterization &,alacritty -e pulsemixer,steam &,LIBVA_DRIVER_NAME=i915 nuclear --enable-gpu-rasterization &}

alt + r ; {p,s}
    {~/bin/rpulse,~/.local/bin/rkd}

super + shift + m
    alacritty -e mocp

super + alt + w
    togglewheel


super + alt + h
    xdotool click 6

super + alt + j
    xdotool click 5

super + alt + k
    xdotool click 4

super + alt + l
    xdotool click 7

super + alt + m
    xdotool click 1

super + alt + t
    alacritty --class todo -e nvim ~/todo.md


super + alt + a
    xdotool mousemove 80 220

super + alt + s
    xdotool mousemove 80 870

super + alt + d
    xdotool mousemove 1830 220

super + alt + f
    xdotool mousemove 1830 870



XF86Sleep
    betterlockscreen -l blur
